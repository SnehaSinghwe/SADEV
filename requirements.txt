# SADEV Backend Requirements
# Install with: pip install -r requirements.txt --break-system-packages
#
# LLM backend: Ollama (local, default) or Anthropic Claude (cloud)
#   - Local:  ollama pull gemma2 && ollama serve
#   - Cloud:  set ANTHROPIC_API_KEY in .env

# ── Web framework ─────────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0
httpx==0.27.0
pydantic==2.7.0
python-dotenv==1.0.1

# ── Ollama client (local LLM for generation + response gen) ──────────────
ollama==0.2.1

# ── Language detection + NLP ──────────────────────────────────────────────
langdetect==1.0.9
indic-nlp-library==0.92

# ── PII scrubbing ─────────────────────────────────────────────────────────
presidio-analyzer==2.2.354
presidio-anonymizer==2.2.354
spacy==3.7.4

# ── LLM clients (cloud fallback) ──────────────────────────────────────────
anthropic==0.28.0
openai==1.30.0

# ── RAG / Embeddings ──────────────────────────────────────────────────────
sentence-transformers==2.7.0
faiss-cpu==1.8.0
numpy==1.26.4

# ── Firebase ─────────────────────────────────────────────────────────────
firebase-admin==6.5.0

# ── MongoDB (async) ───────────────────────────────────────────────────────
motor==3.4.0
pymongo==4.7.2

# ── ML training (only needed for ml/ scripts) ────────────────────────────
torch==2.3.0
transformers==4.41.0
scikit-learn==1.4.2
pandas==2.2.2
datasets==2.19.1

# ── Testing ───────────────────────────────────────────────────────────────
pytest==8.2.0
pytest-asyncio==0.23.6
pytest-cov==5.0.0

# ── Utilities ────────────────────────────────────────────────────────────
accelerate==0.30.0        # for transformers training on GPU
tqdm==4.66.4              # progress bars for generation + training scripts
